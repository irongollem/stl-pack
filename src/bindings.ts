// @ts-nocheck
// eslint-disable
// biome-ignore lint/*: auto-generated file

// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
/**
 * Stage the whole draft into the release directory in CANONICAL layout
 * (Model/Supported|Unsupported[/Variant], sidecar per leaf — see
 * file/stage.rs), then record every leaf in release.json in one write.
 * One command for the whole batch on purpose: members sharing a canonical
 * leaf must merge (poses become file-level metadata), and the old
 * per-model command raced its own release.json read-modify-write.
 */
async addModels(models: StlModel[], releaseDir: string) : Promise<Result<([StlModel, string])[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("add_models", { models, releaseDir }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async createRelease(release: Release, imagePaths: string[], otherFilePaths: string[], licencePath: string | null) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("create_release", { release, imagePaths, otherFilePaths, licencePath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async finalizeRelease(releaseDir: string) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("finalize_release", { releaseDir }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelCompression(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_compression", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Open files with their OS-default application — the "print" hand-off to
 * whatever slicer owns the extension. NOT the opener plugin: its open_path
 * is fire-and-forget (open::that_detached), which reports success even
 * when the OS has no app for the file type, leaving the user with a button
 * that silently does nothing. Launching the OS opener ourselves captures
 * that failure so the UI can react (fall back to revealing the folder).
 */
async openWithDefaultApp(paths: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("open_with_default_app", { paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Import a packed release into the library: verify component checksums,
 * extract (rematerializing dedup-elided names), land it under the same
 * naming scheme create_release uses. A catalog scan afterwards restores
 * the packed curation. `components` limits the run to the named components
 * (selective import / update); None imports everything.
 */
async importRelease(packagePath: string, libraryDir: string, components: string[] | null) : Promise<Result<ImportOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("import_release", { packagePath, libraryDir, components }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Diff a `release.3pk` against the library without touching anything: per
 * component, is it new, changed, unchanged, packed at rest, or missing its
 * archive — and, regardless of that state, how much of it this library
 * already owns SOMEWHERE by checksum. Feeds the selective-import dialog
 * shown before an import runs.
 */
async inspectReleasePackage(packagePath: string, libraryDir: string) : Promise<Result<PackageInspection, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("inspect_release_package", { packagePath, libraryDir }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * "Import what you own": for each named component, extract the sibling
 * archive the normal way when it checks out, otherwise materialize
 * whatever this library already holds by checksum — including a component
 * whose archive isn't present next to the .3pk at all. A partial result
 * still keeps normal catalog rows for what landed; nothing is invented for
 * what didn't, and re-running after the rest turns up completes it.
 */
async recompileReleaseFromLibrary(packagePath: string, libraryDir: string, components: string[] | null) : Promise<Result<RecompileOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("recompile_release_from_library", { packagePath, libraryDir, components }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * WIP releases that never got packed — a successful finalize removes the
 * scratch folder (compression_jobs::perform_compression), so any directory
 * still holding a release.json here is by definition unfinished.
 */
async listReleaseDrafts() : Promise<Result<ReleaseDraftSummary[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_release_drafts") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Rehydrate a WIP release for the builder UI: release.json only carries
 * {id, path} per model (ModelReference), so the rich curation the UI needs
 * (designer/pose/variant/tags/file_poses…) is read back from each model's
 * own model.json sidecar — the same file add_models staged it to.
 */
async loadReleaseDraft(releaseDir: string) : Promise<Result<[Release, StlModel[]], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("load_release_draft", { releaseDir }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getSettings() : Promise<Result<Settings, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_settings") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setSettings(settings: Settings) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_settings", { settings }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getNsfwAccessState() : Promise<Result<NsfwAccessState, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_nsfw_access_state") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async unlockNsfw(pin: string | null) : Promise<Result<NsfwAccessState, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("unlock_nsfw", { pin }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async lockNsfw() : Promise<Result<NsfwAccessState, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("lock_nsfw") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async configureNsfwPin(pin: string) : Promise<Result<NsfwPinSetup, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("configure_nsfw_pin", { pin }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async changeNsfwPin(currentPin: string, newPin: string) : Promise<Result<NsfwAccessState, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("change_nsfw_pin", { currentPin, newPin }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async recoverNsfwPin(recoveryCode: string, newPin: string) : Promise<Result<NsfwPinSetup, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("recover_nsfw_pin", { recoveryCode, newPin }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async removeNsfwPin(currentPin: string) : Promise<Result<NsfwAccessState, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("remove_nsfw_pin", { currentPin }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async detectBlender() : Promise<Result<BlenderInfo, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("detect_blender") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Detection plus the version verdict, in one call the first-run dialog,
 * Settings, and the Render view all share.
 */
async checkBlender() : Promise<Result<BlenderCheck, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("check_blender") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Kick off the managed download; progress arrives as
 * BlenderProvisionStatus events carrying the returned job_id.
 */
async downloadBlender() : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("download_blender") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelBlenderDownload(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_blender_download", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async startRender(parts: string[], options: RenderOptions) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_render", { parts, options }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelRender(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_render", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read an image as a data URL for the branding bake. The webview draws
 * the finished render (plus logo) onto a canvas — but images loaded via
 * the asset: protocol are cross-origin and TAINT the canvas, making
 * toBlob() throw. Data URLs don't, so the bytes take this detour.
 */
async readImageBase64(path: string) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("read_image_base64", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Overwrite an existing render with the branding-composited PNG the
 * webview produced. Three guards keep a bad bake from eating a good
 * render: the target must already exist (this only ever re-writes a
 * finished render), the bytes must carry the PNG magic (a blank/failed
 * canvas export dies here, not on disk), and the write goes through
 * temp + rename so a crash can't leave a half-written file.
 */
async writePngBase64(path: string, data: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("write_png_base64", { path, data }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read a shareable look file for import. The webview's fs capability is
 * read-only and dialog-driven anyway; this narrow command keeps the same
 * posture: .json only, 1 MB cap (a look file is ~1 KB — anything bigger is
 * a mis-pick), parsing/validation stays client-side where the schema lives.
 */
async readLookJson(path: string) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("read_look_json", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Write a shareable look file where the save dialog pointed. Mirrors
 * write_png_base64's guarded style: the contents must parse as JSON (the
 * caller builds them, but a corrupt write would burn the person it was
 * shared WITH), and temp + rename means no half-written file survives.
 */
async writeLookJson(path: string, contents: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("write_look_json", { path, contents }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getPending3dpak() : Promise<string | null> {
    return await TAURI_INVOKE("get_pending_3dpak");
},
async startCatalogScan(root: string) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_catalog_scan", { root }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The configured catalog folders with their indexed footprint. Folders the
 * user added but never scanned report zero counts and no last_scan.
 */
async listCatalogRoots() : Promise<Result<CatalogRootSummary[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_catalog_roots") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Register a catalog folder without scanning it yet. Returns the updated
 * list. Adding is idempotent; a folder nested in (or swallowing) a
 * configured one is rejected — overlapping roots would double-index.
 */
async addCatalogRoot(path: string) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("add_catalog_root", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Drop a catalog folder and purge its slice of the index. The disk is
 * never touched; user tags/metadata for the purged models go with them
 * (their durable home is the model.json sidecars), so re-adding the
 * folder later just means a rescan.
 */
async removeCatalogRoot(path: string) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("remove_catalog_root", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Choose (or clear, with None) the staging folder Clean up drains the
 * other folders into. Must already be a configured catalog folder.
 */
async setPrimaryCatalogRoot(path: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_primary_catalog_root", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async startDuplicateScan() : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_duplicate_scan") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Runs as a catalog job; excluded while a pack job could be deleting
 * the loose bytes mining reads.
 */
async startGeometryScan() : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_geometry_scan") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * What "Auto" would pick, without overwriting the stored setting.
 */
async getRecommendedEdgeCap() : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_recommended_edge_cap") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelCatalogJob(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_catalog_job", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async searchCatalog(query: string, tags: string[], geometry: GeometryRange, limit: number, offset: number) : Promise<Result<CatalogSearchResult, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("search_catalog", { query, tags, geometry, limit, offset }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getCatalogTags() : Promise<Result<TagCount[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_catalog_tags") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async addCatalogTag(dirPath: string, tag: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("add_catalog_tag", { dirPath, tag }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async removeCatalogTag(dirPath: string, tag: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("remove_catalog_tag", { dirPath, tag }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Tag/untag every member of a group at once — a tag describes the mini,
 * not one support build of it.
 */
async addGroupTag(groupName: string, tag: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("add_group_tag", { groupName, tag }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async removeGroupTag(groupName: string, tag: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("remove_group_tag", { groupName, tag }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getCatalogModelFiles(dirPath: string, variantKey: string | null) : Promise<Result<CatalogFile[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_catalog_model_files", { dirPath, variantKey }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getModelGeometry(dirPath: string) : Promise<Result<ModelFileGeometry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_model_geometry", { dirPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getCatalogStats() : Promise<Result<CatalogStats, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_catalog_stats") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getDuplicateGroups() : Promise<Result<DuplicateGroup[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_duplicate_groups") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getCatalogReleases() : Promise<Result<ReleaseSummary[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_catalog_releases") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getCatalogDesigners() : Promise<Result<DesignerCount[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_catalog_designers") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async renameCatalogDesigner(oldName: string, newName: string) : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("rename_catalog_designer", { oldName, newName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async renameCatalogRelease(designer: string, oldName: string, newName: string) : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("rename_catalog_release", { designer, oldName, newName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Update one member's metadata, then propagate the shared facets (variant,
 * pose, scale) to its support twins — the supported/unsupported builds of
 * the same sculpt, matched by exact path structure. Returns how many twins
 * received the edit so the UI can say so. Only Some values propagate;
 * clears stay local to the edited member.
 */
async updateModelMetadata(dirPath: string, meta: ModelMetaUpdate) : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("update_model_metadata", { dirPath, meta }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Copy an image into the app's previews dir and point the model at it.
 * The copy (not a reference) is deliberate: render outputs and picked
 * images live wherever the user left them and may be cleaned up; the
 * catalog preview must not die with them. The filename is a stable
 * per-model hash plus a timestamp — a fresh URL each time, because the
 * webview caches aggressively by URL, with older copies swept first.
 */
async setModelPreview(dirPath: string, imagePath: string, variantKey: string | null) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_model_preview", { dirPath, imagePath, variantKey }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async deleteDuplicateFiles(filePaths: string[]) : Promise<Result<BatchOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("delete_duplicate_files", { filePaths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Delete whole models: from the index always, from disk optionally. Disk
 * deletion tries the OS trash / Recycle Bin first so a wrong click stays
 * reversible, and falls back to a permanent delete where no trash exists
 * (network shares, most NAS mounts) — the confirmation dialog is the real
 * safeguard, and refusing to delete on trash-less volumes would strand
 * exactly the libraries this app targets. The index only forgets a model
 * whose disk delete succeeded (or that was already gone), so the catalog
 * keeps telling the truth about what's still on disk. Catalog-only
 * removal (delete_files = false) is a SOFT remove: the folders stay on
 * disk but go on the scan-ignore list, so a rescan doesn't quietly undo
 * the user's decision — Settings shows the list and can take them back.
 */
async deleteModels(dirPaths: string[], deleteFiles: boolean) : Promise<Result<DeleteOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("delete_models", { dirPaths, deleteFiles }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async summarizeModelDirs(dirPaths: string[]) : Promise<Result<DeleteSummary, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("summarize_model_dirs", { dirPaths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listIgnoredFolders() : Promise<Result<IgnoredFolder[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_ignored_folders") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Take a folder off the soft-remove list. The models come back on the
 * next scan of their root — the marker was the only thing hiding them.
 */
async unignoreFolder(dirPath: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("unignore_folder", { dirPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Flag or unflag every model behind one or more card names — the drawer's
 * "mark 18+" button and the batch bar's equivalent both land here. Resolves
 * through group_members with include_nsfw=true so an ALREADY-hidden member
 * still gets updated (unmarking a mixed group must reach every variant, not
 * just the ones currently visible).
 */
async setGroupNsfw(groupNames: string[], nsfw: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_group_nsfw", { groupNames, nsfw }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listNsfwDesigners() : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_nsfw_designers") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setDesignerNsfw(designer: string, nsfw: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_designer_nsfw", { designer, nsfw }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Merge a duplicate group: every path in `duplicate_paths` becomes another
 * name for `keep_path`'s bytes (a hardlink), freeing the copies while every
 * variant keeps a working file. The catalog's identities are updated in
 * place so the group reports "shared" without waiting for a rescan.
 */
async mergeDuplicateFiles(keepPath: string, duplicatePaths: string[]) : Promise<Result<BatchOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("merge_duplicate_files", { keepPath, duplicatePaths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Probe whether the volume holding `path` supports hardlink merging.
 * Consulted by the duplicates panel so link-less filesystems (exFAT, some
 * NAS mounts) get delete-only instead of a button that can't work.
 */
async supportsFileLinks(path: string) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("supports_file_links", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async batchMoveModels(operations: MoveOperation[]) : Promise<Result<BatchOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("batch_move_models", { operations }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Dry-run the normalizer: what would move where to make the disk match
 * the curated catalog. Read-only — nothing happens until apply.
 */
async planNormalize(designer: string | null, group: string | null) : Promise<Result<NormalizePlan, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("plan_normalize", { designer, group }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Execute approved normalizer moves. The frontend sends these in chunks
 * so a big NAS batch shows progress and stays cancellable between calls.
 */
async applyNormalize(ops: NormalizeOp[]) : Promise<Result<BatchOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("apply_normalize", { ops }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * After the moves: write authoritative model.json sidecars, sweep emptied
 * dirs, rebuild search. Returns human-readable warnings.
 */
async finalizeNormalize(groupNames: string[], oldDirs: string[]) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("finalize_normalize", { groupNames, oldDirs }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async searchCatalogGroups(query: string, tags: string[], designer: string | null, geometry: GeometryRange, sort: string | null, limit: number, offset: number) : Promise<Result<CatalogGroupResult, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("search_catalog_groups", { query, tags, designer, geometry, sort, limit, offset }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getCatalogGroupMembers(groupName: string) : Promise<Result<CatalogEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_catalog_group_members", { groupName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The scanner-level groups shown under one card. Length > 1 means the card
 * was combined (or renamed into a collision) and can be split — the UI
 * offers "split" exactly then, and rename-to-empty performs it.
 */
async getCatalogGroupSources(groupName: string) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_catalog_group_sources", { groupName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Where a rename/combine of `group_name` would actually reach — call this
 * before committing one, so the UI can warn when a generic scanner-derived
 * name ("Spear") turns out to also belong to an unrelated designer/release
 * (see the group_renames CREATE TABLE comment: no root/designer scoping).
 */
async getGroupRenameOrigins(groupName: string) : Promise<Result<GroupOrigin[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_group_rename_origins", { groupName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove one mis-combined source from a card (its rename row) so it comes
 * back as its own card — the surgical undo, next to full split.
 */
async detachCatalogGroupSource(groupName: string, sourceGroup: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("detach_catalog_group_source", { groupName, sourceGroup }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Reset a card's auto-config: clear the scanner-inferred variant/pose on
 * every member and drop every per-file pose assignment, collapsing the card
 * to one flat file list the user can re-file by hand. Returns the number of
 * file assignments cleared. Nothing moves on disk.
 */
async flattenCatalogGroup(groupName: string) : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("flatten_catalog_group", { groupName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Pick which member's image fronts the group's card.
 */
async setGroupCover(groupName: string, dirPath: string, variantKey: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_group_cover", { groupName, dirPath, variantKey }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async renameCatalogGroup(groupName: string, newName: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("rename_catalog_group", { groupName, newName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async combineCatalogGroups(groupNames: string[], targetName: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("combine_catalog_groups", { groupNames, targetName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Assign files to a pose (with optional per-file support) so a dump
 * folder can be split into pose members. Metadata only — nothing moves on
 * disk. Returns the number of known files assigned.
 */
async assignFilesToPose(paths: string[], variant: string | null, pose: string | null, supportStatus: string | null) : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("assign_files_to_pose", { paths, variant, pose, supportStatus }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Revert files to plain members of their folder. Returns how many
 * assignments existed — 0 tells the UI the selection was never filed.
 */
async clearFilePose(paths: string[]) : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("clear_file_pose", { paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The pose assignments under a folder, for the split UI.
 */
async getFileVariants(dirPath: string) : Promise<Result<FileVariant[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_file_variants", { dirPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Compress each model dir into a model.plinthpack (compressed at rest),
 * sequentially — per-model atomicity keeps the crash surface to one model,
 * and a cancelled or crashed batch resumes by re-running the same selection
 * (already-packed models just finish their bookkeeping and count as done).
 * Cancel via cancel_catalog_job; progress via PackStatus events.
 */
async packModels(modelDirs: string[]) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("pack_models", { modelDirs }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Restore packed models to loose files (archive + sidecar removed), the
 * mirror of pack_models: sequential, cancellable between models, index
 * updated per model so no rescan is needed.
 */
async unpackModels(modelDirs: string[]) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("unpack_models", { modelDirs }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Which model folders a bulk pack would touch: everything with loose model
 * files under the given designer facet and/or checked group names. The
 * frontend shows the count in the confirm dialog, then feeds the same list
 * to pack_models — one resumable job for a whole designer.
 */
async getPackCandidates(designer: string | null, groups: string[]) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_pack_candidates", { designer, groups }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Make `paths` readable on disk, extracting packed ones from their
 * archives as EPHEMERAL working copies — the archive stays authoritative
 * and cleanup_ephemeral_files takes the copies back. Awaits completion:
 * the promise resolving means the bytes are there, so callers just chain
 * their print/preview/render after it. Progress rides the PackStatus
 * stream (phase "extract"); the Started event carries the job_id the
 * frontend can feed to cancel_catalog_job.
 */
async ensureModelFiles(paths: string[]) : Promise<Result<EnsureOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("ensure_model_files", { paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Take back the working copies ensure_model_files materialized — the
 * requested paths, or every live extract when the list is empty (the
 * app-exit sweep). Files that changed since extraction are reported and
 * kept: they're the user's data now (saved supports, edits).
 */
async cleanupEphemeralFiles(paths: string[]) : Promise<Result<BatchOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cleanup_ephemeral_files", { paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async startBatchRender(targets: BatchRenderTarget[]) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_batch_render", { targets }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Everything the batch confirm dialog needs, resolved through
 * group_members (NOT raw SQL: expand_file_variants is what gives fanned
 * members their own variant_key + per-variant preview state).
 */
async getRenderCandidates(designer: string | null, groups: string[]) : Promise<Result<RenderCandidate[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_render_candidates", { designer, groups }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the orientation a render used ("the render IS the chosen
 * orientation") — the studio calls this after a successful catalog-preview
 * render, and batch renders read it back so re-renders never need
 * repositioning.
 */
async setModelRotation(dirPath: string, rotation: [number, number, number]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_model_rotation", { dirPath, rotation }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Find minihoard and prove it runs (`--version`). None simply means the
 * egg stays hidden.
 */
async detectMinihoard() : Promise<MinihoardInfo | null> {
    return await TAURI_INVOKE("detect_minihoard");
},
/**
 * Launch one whitelisted minihoard run and stream its output as events.
 * Returns the job id immediately; MinihoardStatus::Finished closes it.
 */
async runMinihoard(binaryPath: string, args: string[]) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("run_minihoard", { binaryPath, args }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Kill the active run (the "stop" button). Emits Finished(success=false).
 */
async cancelMinihoard(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_minihoard", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Probe minihoard's install + auth health (`status --json`). Never surfaces
 * auth failure as an error — a dead login or missing cookie is reported in the
 * returned fields, so the UI can render state rather than an exception.
 */
async minihoardStatus(binaryPath: string) : Promise<Result<MinihoardHealth, MinihoardError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("minihoard_status", { binaryPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Fetch the whole library (`list --json`). Buffered — ~4k entries is fine, and
 * objectPreviews has no pagination, so this is one call, not a poll. A stale or
 * missing cookie comes back as a typed `CookieExpired`/`CookieMissing`.
 */
async minihoardList(binaryPath: string) : Promise<Result<MinihoardEntry[], MinihoardError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("minihoard_list", { binaryPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Fetch one object's detail (page url + preview image) for a row expand.
 * Needs minihoard >= 0.4.1 (the `object` subcommand); on an older binary this
 * fails, and the caller simply shows the row without a preview.
 */
async minihoardObject(binaryPath: string, id: number) : Promise<Result<MinihoardObject, MinihoardError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("minihoard_object", { binaryPath, id }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Start a typed download of the given object ids. Returns a job id immediately;
 * progress arrives as `MinihoardDownloadStatus` events. One run at a time.
 */
async startMinihoardDownload(binaryPath: string, ids: number[]) : Promise<Result<string, MinihoardError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_minihoard_download", { binaryPath, ids }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Cancel the active typed download (the queue's stop button). Emits
 * `Cancelled` and marks the run terminal so the waiter stays silent.
 */
async cancelMinihoardDownload(jobId: string) : Promise<Result<null, MinihoardError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_minihoard_download", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getCutterLibrary() : Promise<Cutter[]> {
    return await TAURI_INVOKE("get_cutter_library");
},
/**
 * The caliper-measured plinth profile (see `PlinthParams::default`'s doc
 * comment) as a command, so the frontend's "new job" defaults come from
 * this one Rust value instead of a hand-copied literal that could drift
 * from it.
 */
async getPlinthDefaults() : Promise<PlinthParams> {
    return await TAURI_INVOKE("get_plinth_defaults");
},
async startBaseCut(job: BaseCutJob) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_base_cut", { job }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelBaseCut(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_base_cut", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async exportCutsToCatalog(artifacts: CutCatalogArtifact[], root: string, collection: string) : Promise<Result<CutCatalogExportSummary, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("export_cuts_to_catalog", { artifacts, root, collection }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repairPlinthBaseExports() : Promise<Result<PlinthRepairSummary, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repair_plinth_base_exports") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getLandscapePresets() : Promise<GeneratorPreset[]> {
    return await TAURI_INVOKE("get_landscape_presets");
},
/**
 * `preset_id` is the preset chip's id ("cobblestone-street", ...) when
 * starting from a chip, None for a from-scratch custom bake — only used to
 * name the output file (docs/BASECUTTER.md:
 * "landscapes/<preset-or-custom>-<seed>.stl").
 */
async startLandscapeGeneration(params: LandscapeParams, presetId: string | null) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_landscape_generation", { params, presetId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelLandscapeGeneration(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_landscape_generation", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Bundled scatter asset set (docs/SCATTER.md "Bundled assets"): S4a
 * curation output — see `scatter_assets::BUNDLED_ASSETS` for the pinned
 * id/label/footprint/height/license table this reads, and its own doc
 * comment for how that table is kept from drifting off the curated
 * manifest.json shipped alongside the STLs. Each asset is materialized
 * lazily (same as the embedded scripts) on every call, so a stale
 * materialized copy can never survive a rebuild.
 */
async getScatterAssets() : Promise<Result<ScatterAsset[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_scatter_assets") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The curated CREDITS.md, embedded verbatim (docs/SCATTER.md "Bundled
 * assets": "listed in an in-app credits panel + CREDITS file when
 * attribution is owed"). Every bundled piece is CC0 (nothing legally
 * owed) EXCEPT the CC BY-SA 4.0 leaf set, whose attribution the file
 * carries — see the file itself. Exposed as its own command rather than folded into
 * `ScatterAsset` since it's one shared document, not a per-asset field.
 */
async getScatterCredits() : Promise<string> {
    return await TAURI_INVOKE("get_scatter_credits");
},
/**
 * Scan `dir` (non-recursive) for `*.stl` files and measure each one's
 * bounding box in pure Rust — no Blender (docs/SCATTER.md "User library").
 * A file that fails to parse is not dropped from the result: it's returned
 * with zeroed dims and a `warning` explaining why, so the UI can still
 * list it (and the user can see WHY it's unusable) instead of a silent
 * gap between "files in the folder" and "pieces offered".
 */
async scanScatterLibrary(dir: string) : Promise<Result<ScatterAsset[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("scan_scatter_library", { dir }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async startScatter(job: ScatterJob) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("start_scatter", { job }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelScatter(jobId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_scatter", { jobId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/


export const events = __makeEvents__<{
baseCutStatus: BaseCutStatus,
batchRenderStatus: BatchRenderStatus,
blenderProvisionStatus: BlenderProvisionStatus,
compressionStatus: CompressionStatus,
duplicateStatus: DuplicateStatus,
geometryStatus: GeometryStatus,
landscapeGenStatus: LandscapeGenStatus,
minihoardDownloadStatus: MinihoardDownloadStatus,
minihoardStatus: MinihoardStatus,
packStatus: PackStatus,
renderStatus: RenderStatus,
scanStatus: ScanStatus,
scatterStatus: ScatterStatus
}>({
baseCutStatus: "base-cut-status",
batchRenderStatus: "batch-render-status",
blenderProvisionStatus: "blender-provision-status",
compressionStatus: "compression-status",
duplicateStatus: "duplicate-status",
geometryStatus: "geometry-status",
landscapeGenStatus: "landscape-gen-status",
minihoardDownloadStatus: "minihoard-download-status",
minihoardStatus: "minihoard-status",
packStatus: "pack-status",
renderStatus: "render-status",
scanStatus: "scan-status",
scatterStatus: "scatter-status"
})

/** user-defined constants **/



/** user-defined types **/

export type AppError = { InvalidInput: string } | { IoError: string } | { JsonError: string } | { FileProcessingError: string } | { ConfigError: string } | { NotFoundError: string } | { ImageProcessingError: string } | { UserCancelled: string }
export type BaseCutCancelledStatus = { job_id: string }
export type BaseCutCutDoneStatus = { job_id: string; index: number; out_path: string; dims_mm: [number, number, number]; manifold: boolean; 
/**
 * The union tripwire (normal seat-on-plinth mode only, see
 * docs/BASECUTTER.md "The cut pipeline"): `Some(false)` when the
 * plug/plinth union left more than one loose shell behind — the exact
 * silent failure a base-cut accident revealed (CUT_DONE reported
 * success, the STL held two loose shells). The cut still counts as
 * success — the mesh may still be printable — this only makes the
 * silent case visible. `None` in topper mode (nothing to fuse) or when
 * the union fused into one shell.
 */
fused: boolean | null; 
/**
 * Loose-shell count backing `fused`, present alongside it.
 */
shells: number | null; 
/**
 * Present only when the job's requested `topper_mm` fell outside
 * base_cut.py's [1.0, 3.0] clamp range — the effective value the
 * script used instead.
 */
topper_mm_clamped: number | null; 
/**
 * `Some(true)` = this placement carried a magnet spec that topper mode
 * ignored (there's no plinth to pocket it into).
 */
magnet_ignored: boolean | null; 
/**
 * Slice-mode scatter shells omitted because they could not be clipped
 * into a closed, rim-bounded solid.
 */
scatter_skipped: number | null; 
/**
 * VTT GLB export design doc "Base cut": the cut's `.glb` twin path,
 * glb-mode jobs only (`BaseCutJob.glb == true`) — `None` in the
 * default (non-glb) mode.
 */
glb_path: string | null }
export type BaseCutCutFailedStatus = { job_id: string; index: number; reason: string }
export type BaseCutCutStartedStatus = { job_id: string; index: number }
export type BaseCutFailedStatus = { job_id: string; message: string; 
/**
 * Last ~10 lines of Blender stdout — a post-mortem when the failure
 * wasn't a clean CUT_FAILED/VALIDATION_FAILED token (e.g. a crash).
 */
stdout_tail: string }
export type BaseCutFinishedStatus = { job_id: string; ok_count: number; total: number }
/**
 * A base-cut job, as sent from the frontend and forwarded to base_cut.py.
 * Field names/renames match the script's job JSON verbatim (see its
 * top docstring and docs/BASECUTTER.md "Pinned interfaces") — `landscape`
 * and `out_dir` are the script's exact keys.
 */
export type BaseCutJob = { landscape: string; placements: Placement[]; plinth: PlinthParams; out_dir: string; 
/**
 * `Some(t)` = BASE TOPPER mode: no plinth at all — the plug is
 * flat-trimmed `t` mm below its lowest sculpted point and exported as a
 * glue-on terrain slab for hard plastic bases. `None` = the normal
 * seat-on-plinth flow. See docs/BASECUTTER.md "Pinned interfaces" for
 * the full contract (clamp range, magnet handling). `serde(default)` so
 * old frontends/job files without the field keep working.
 */
topper_mm?: number | null; 
/**
 * See `ScatterRim`. `#[serde(default)]` is protocol hygiene for
 * DESERIALIZING a JSON blob Rust didn't produce itself (a hand-edited
 * job file, a minimal test fixture) — it is not there to support any
 * particular vintage of file. Rust is the ground truth for what
 * actually reaches the script: `BaseCutJob` is never constructed
 * without deciding this field (it's not an `Option`), so every job
 * Rust serializes carries an explicit `"scatter_rim"` key, never an
 * absent one — see `job_always_serializes_scatter_rim_explicitly`
 * below, which pins that as the one ground truth so the two defaults
 * (this one, and base_cut.py's own lenient `job.get("scatter_rim",
 * "keep")` read) can't drift silently against each other.
 */
scatter_rim?: ScatterRim; 
/**
 * VTT GLB export design doc "Base cut": when true, base_cut.py imports
 * the landscape's `.glb` twin (swap extension of `landscape_path`)
 * instead of the bare STL, paints/repairs colors through every cut,
 * and exports a `.glb` twin alongside each cut STL (`CutDone.glb`
 * below). `false` (the default) is today's behavior EXACTLY — see
 * base_cut.py's own docstring "glb mode" section for the full
 * contract. `#[serde(default)]` so old job files/frontends without
 * the key keep working.
 */
glb?: boolean }
export type BaseCutStartedStatus = { job_id: string; total: number }
/**
 * Base Cutter job progress — see docs/BASECUTTER.md "Pinned interfaces".
 * Shaped like BatchRenderStatus (started / per-step progress / finished /
 * failed / cancelled), but the steps mirror base_cut.py's own token
 * protocol (VALIDATING / VALIDATED / CUT_START / CUT_DONE / CUT_FAILED /
 * JOB_DONE) rather than render's sample-progress model. Cancellation gets
 * its own variant (mirroring BatchRenderCancelledStatus) rather than
 * flowing through Failed: commands.rs's run_base_cut_job matches
 * AppError::UserCancelled specifically so the frontend can tell "the user
 * stopped this" apart from "this actually broke".
 */
export type BaseCutStatus = { Started: BaseCutStartedStatus } | { Validating: BaseCutValidatingStatus } | { Validated: BaseCutValidatedStatus } | { CutStarted: BaseCutCutStartedStatus } | { CutDone: BaseCutCutDoneStatus } | { CutFailed: BaseCutCutFailedStatus } | { Finished: BaseCutFinishedStatus } | { Failed: BaseCutFailedStatus } | { Cancelled: BaseCutCancelledStatus }
export type BaseCutValidatedStatus = { job_id: string; report: BaseCutValidationReport }
export type BaseCutValidatingStatus = { job_id: string }
/**
 * Mirrors base_cut.py's `validate()` report dict exactly (see its
 * docstring): non-manifold edge count, bounding box, vertex count, and an
 * optional warning string added only when the landscape failed the check
 * but the script kept going anyway (the "Spike policy" in base_cut.py's
 * main(): report loudly, keep cutting, let the app-side gate harden later).
 */
export type BaseCutValidationReport = { non_manifold_edges?: number; dims_mm?: [number, number, number]; verts?: number; warning?: string | null }
/**
 * Outcome of a batch that may partially succeed — the counts and the
 * per-item errors travel together so the UI can report both.
 */
export type BatchOutcome = { succeeded: number; errors: string[] }
export type BatchRenderCancelledStatus = { job_id: string; succeeded: number }
export type BatchRenderCompletedStatus = { job_id: string; succeeded: number; failed: number; total_models: number; elapsed_seconds: number }
export type BatchRenderFailedStatus = { job_id: string; error: string; 
/**
 * Models finished before the failure — their previews are already
 * persisted and real.
 */
succeeded: number }
export type BatchRenderModelStatus = { job_id: string; model_index: number; dir_path: string; variant_key: string | null; ok: boolean; error: string | null }
export type BatchRenderProgressStatus = { job_id: string; 
/**
 * Display name of the model currently rendering.
 */
current_model: string; 
/**
 * 1-based position in the batch.
 */
model_index: number; total_models: number; 
/**
 * Cycles sample progress of the current model.
 */
model_percent: number; 
/**
 * Whole-batch percent: (finished*100 + model_percent) / total.
 */
percent: number }
export type BatchRenderStartedStatus = { job_id: string; total_models: number }
/**
 * Batch preview rendering: many models, ONE Blender launch. Deliberately
 * its own stream — RenderStatus is the studio's single-job protocol and
 * batch events flowing through it would hijack that UI.
 */
export type BatchRenderStatus = { Started: BatchRenderStartedStatus } | { Progress: BatchRenderProgressStatus } | 
/**
 * One model finished (ok or not) — previews land incrementally, so the
 * catalog can refresh without waiting for the whole sweep.
 */
{ ModelFinished: BatchRenderModelStatus } | { Completed: BatchRenderCompletedStatus } | { Failed: BatchRenderFailedStatus } | { Cancelled: BatchRenderCancelledStatus }
/**
 * One model of the batch, as the frontend selected it (a RenderCandidate
 * the user confirmed). rotation is the stored "x,y,z" or null → 90,0,0.
 */
export type BatchRenderTarget = { dir_path: string; variant_key: string | null; name: string; parts: string[]; rotation: string | null }
export type BlenderCheck = { verdict: BlenderVerdict; 
/**
 * None only when Missing
 */
info: BlenderInfo | null; 
/**
 * The pinned version the download pipeline would install — dialog copy
 * and the first-run acknowledgement key
 */
managed_version: string; 
/**
 * Whether the detected binary is one we downloaded ourselves
 */
is_managed: boolean }
export type BlenderInfo = { path: string; version: string }
export type BlenderProvisionStatus = { Started: ProvisionStartedStatus } | { Progress: ProvisionProgressStatus } | 
/**
 * Post-download work; Progress stops once the bytes are all here.
 */
{ Extracting: ProvisionExtractingStatus } | { Completed: ProvisionCompletedStatus } | { Failed: ProvisionFailedStatus } | { Cancelled: ProvisionCancelledStatus }
/**
 * How a detected Blender measures against the render gate
 * (provision::MIN_VERSION / RECOMMENDED_VERSION). Only Missing and TooOld
 * disable rendering; Outdated is a suggestion the user may dismiss.
 */
export type BlenderVerdict = "Missing" | "TooOld" | "Outdated" | "Ok"
/**
 * N seeded rocks — lumpy elliptical footprints with a per-boulder
 * superellipse height profile (steep-shouldered, flat-topped plateau, not
 * a gaussian dome) and noise-displaced surface grain, combined by max (not
 * sum) so overlapping boulders read as touching stones rather than a
 * stacked tower (see gen_landscape.py's _boulders_layer docstring — that's
 * where all the shape tuning lives, this struct is just count/size/amount).
 */
export type BouldersLayer = { enabled?: boolean; count: number; min_mm: number; max_mm: number; amount?: number }
/**
 * Parabolic crown across the plate's width — cobblestone streets are
 * highest at the centerline, sloping to the gutters at the edges.
 */
export type CamberLayer = { enabled?: boolean; amount?: number }
export type CancelledStatus = { job_id: string }
export type CatalogEntry = { dir_path: string; 
/**
 * Effective display name: the user's custom_name when set, else the
 * scanner's. custom_name below carries the raw override so the UI can
 * distinguish "renamed" from "inferred" (and clear it to revert).
 */
name: string; description: string | null; designer: string | null; release_name: string | null; preview_path: string | null; tags: string[]; file_count: number; total_size_bytes: number; 
/**
 * The facet between support and pose — a weapon/sculpt option
 * (sword, mounted, …). Free-text, user-supplied (no scanner inference).
 */
variant: string | null; pose: string | null; scale: string | null; support_status: string | null; release_date: string | null; custom_name: string | null; 
/**
 * The studio/brand. Scanned from the release for release models, and
 * user-overridable per model. sculptor (the individual artist) has no
 * folder signal, so it comes only from the user or an imported manifest.
 */
sculptor: string | null; 
/**
 * Base sizes in mm: "25", or "60x35" for ovals/rectangles.
 */
base_round_mm: string | null; base_square_mm: string | null; 
/**
 * Set only on members synthesized from file→pose assignments: a stable
 * `{dir_path}\u{1f}{pose}` handle ("...\u{1f}" for the residual
 * unassigned member). None means a whole-folder member — its dir_path
 * alone identifies it. The UI keys members on `variant_key ?? dir_path`
 * and passes it back to fetch only that pose's files.
 */
variant_key: string | null; 
/**
 * The scanner-level group this member belongs to — the unit a combine
 * maps and a detach removes. Differs from the card's group_name when
 * the member is only in the card via a rename/combine.
 */
source_group: string; 
/**
 * True when every model file in this member's folder lives inside a
 * pack archive (compressed at rest) — byte-needing actions must unpack
 * or extract first.
 */
packed?: boolean; 
/**
 * Chosen render orientation "x,y,z" (Blender euler degrees) — saved
 * when the studio renders this model; batch re-renders reuse it.
 */
rotation?: string | null; 
/**
 * Measured printed size "60.2x35.1x88.7" in mm, and how many part
 * files make up the model — captured by the render pipeline.
 */
dims_mm?: string | null; part_count?: string | null; 
/**
 * This member's effective 18+ flag (explicit override, or the
 * designer-wide rule when unset) — a display filter, not encryption;
 * scans still index everything. See db::NSFW_EFFECTIVE_SQL.
 */
nsfw?: boolean }
export type CatalogFile = { path: string; file_name: string; extension: string; size_bytes: number; 
/**
 * True when the bytes live inside the model's pack archive — `path` is
 * then where the file lands when extracted, not a file on disk.
 */
packed?: boolean }
/**
 * One logical model: every variant dir (supported/unsupported builds,
 * poses) sharing a group_name, aggregated for the card view. Drill in
 * with get_catalog_group_members.
 */
export type CatalogGroup = { group_name: string; designer: string | null; 
/**
 * Aggregated like designer: any variant's value stands in for the
 * group, so grouping by release works even when only the release
 * models (not later additions) carry the metadata.
 */
release_name: string | null; release_date: string | null; variant_count: number; pose_count: number; support_statuses: string[]; file_count: number; total_size_bytes: number; preview_path: string | null; 
/**
 * True when every member of the group is compressed at rest.
 */
packed?: boolean; 
/**
 * True when ANY member is effectively 18+ — the card-level badge and
 * what the browse filter hides when Settings' "Show 18+" is off.
 */
nsfw?: boolean; 
/**
 * Tallest member's z-extent in mm, MAX across the group. None means no
 * member has mined geometry yet.
 */
height_mm?: number | null; 
/**
 * Total print volume in mm³, SUM across the group's mined files. None
 * means no member has mined geometry yet.
 */
volume_mm3?: number | null }
export type CatalogGroupResult = { groups: CatalogGroup[]; total: number; 
/**
 * Groups hidden by an active height/volume filter purely for lack of
 * mined geometry — see db::search_groups' not_mined_count doc.
 */
not_mined_count?: number }
/**
 * One configured catalog folder and its indexed footprint — a row in the
 * roots management UI. Zero counts with no last_scan mean "added but never
 * scanned"; a stale last_scan is the cue to offer a rescan.
 */
export type CatalogRootSummary = { root: string; model_count: number; file_count: number; total_size_bytes: number; last_scan_epoch: number | null; 
/**
 * The optional staging folder: Clean up drains the other folders'
 * models into this one. At most one row carries it.
 */
primary: boolean }
export type CatalogSearchResult = { entries: CatalogEntry[]; total: number }
export type CatalogStats = { total_models: number; total_files: number; total_size_bytes: number; extensions: ExtensionStat[]; last_scan_epoch: number | null; 
/**
 * Compressed-at-rest accounting: how many models are packed, what their
 * files would occupy loose, and what the archives actually take.
 * total_size_bytes reports logical sizes, so the UI derives real disk
 * usage as total − packed_logical + packed_archive.
 */
packed_models?: number; packed_logical_bytes?: number; packed_archive_bytes?: number }
export type CompletedStatus = { job_id: string; total_files: number; total_size_kb: number; elapsed_seconds: number; folder_path: string }
/**
 * How one incoming component compares to what the library already holds.
 */
export type ComponentState = 
/**
 * Not in the local release (or the release isn't imported yet).
 */
"new" | 
/**
 * Local manifest lists a different archive checksum — an update.
 */
"changed" | 
/**
 * Same checksum both sides; nothing to do.
 */
"unchanged" | 
/**
 * The local copy is packed at rest — unpack before updating.
 */
"packed" | 
/**
 * The component archive isn't next to the .3pk; can't import.
 */
"missing_archive"
export type ComponentStatus = { name: string; state: ComponentState; 
/**
 * f64 (not the manifest's u64): specta refuses integer types TypeScript
 * numbers can't hold, same convention as the catalog's size fields.
 */
size_bytes: number; file_count: number; 
/**
 * Display names of the models inside (custom name when set).
 */
model_names: string[]; 
/**
 * Why the component can't be imported, for Packed/MissingArchive.
 */
detail: string | null; 
/**
 * How many of `file_count` this library already owns SOMEWHERE, by
 * checksum — independent of `state`: even a MissingArchive component's
 * checksums are known from the manifest itself.
 */
files_owned: number; missing_bytes: number; missing: MissingFile[] }
export type CompressionStatus = { Started: StartedStatus } | { Progress: ProgressStatus } | { Completed: CompletedStatus } | { Failed: FailedStatus } | { Cancelled: CancelledStatus }
export type CompressionType = "SevenZip" | "Zip"
/**
 * A successful cut as it crosses from the transient Blender output area
 * into the durable catalog. Identity and footprint travel explicitly — a
 * filename collision suffix is storage trivia, never model metadata.
 */
export type CutCatalogArtifact = { id: string; source_path: string; cutter: CutterKind; mode: CutCatalogMode }
export type CutCatalogExportSummary = { release_dir: string; added: number; updated: number; unchanged: number; repaired: number; warnings: string[] }
export type CutCatalogMode = "base" | "topper"
/**
 * A standard-library cutter (or a later user-defined one). Dimensions are
 * the NOMINAL (bottom-face, table-touching) footprint — the smaller cut
 * footprint is always derived from these via `top_face_of`, never stored.
 */
export type Cutter = { id: string; label: string; kind: CutterKind }
/**
 * Cutter footprint shapes. Internally tagged on `kind` with lowercase
 * variant names so the JSON matches `base_cut.py`'s job format verbatim
 * (`{"kind": "circle", "diameter_mm": 32.0}` — see its top docstring and
 * docs/BASECUTTER.md "Pinned interfaces"). Open for extension: a
 * user-traced outline is a later variant, not a rewrite of this type.
 */
export type CutterKind = { kind: "circle"; diameter_mm: number } | { kind: "ellipse"; major_mm: number; minor_mm: number } | { kind: "rect"; width_mm: number; depth_mm: number }
/**
 * How a model deletion went. BatchOutcome plus one distinction the UI must
 * surface: hard_deleted counts folders that skipped the trash. The confirm
 * dialog promises recoverability, so when a volume couldn't deliver it the
 * user hears that it didn't — after the fact, but truthfully.
 */
export type DeleteOutcome = { succeeded: number; hard_deleted: number; errors: string[] }
/**
 * What a pending model deletion covers, for the confirmation dialog:
 * counted from the index with the same prefix scoping the deletion uses,
 * so the dialog describes exactly what delete_models will take.
 */
export type DeleteSummary = { dir_count: number; file_count: number; total_bytes: number }
/**
 * One designer and how many logical models (groups) carry that name —
 * feeds the catalog's designer filter dropdown.
 */
export type DesignerCount = { designer: string; model_count: number }
export type DuplicateCancelledStatus = { job_id: string }
export type DuplicateCompletedStatus = { job_id: string; group_count: number; wasted_bytes: number }
export type DuplicateFailedStatus = { job_id: string; error: string }
export type DuplicateGroup = { hash: string; size_bytes: number; paths: string[]; 
/**
 * How many physical copies the paths actually occupy on disk. Hardlinked
 * paths count once, so a fully merged group reports 1 ("shared", nothing
 * to reclaim) and reclaimable space is size_bytes × (distinct_copies − 1).
 */
distinct_copies: number; 
/**
 * The subset of `paths` whose bytes live inside a pack archive — they
 * join detection via their stored checksums but can't be merged or
 * deleted until the model is unpacked; the UI greys them with a hint.
 */
packed_paths?: string[] }
export type DuplicateProgressStatus = { job_id: string; processed: number; total: number }
export type DuplicateStartedStatus = { job_id: string }
export type DuplicateStatus = { Started: DuplicateStartedStatus } | { Progress: DuplicateProgressStatus } | { Completed: DuplicateCompletedStatus } | { Failed: DuplicateFailedStatus } | { Cancelled: DuplicateCancelledStatus }
/**
 * What ensure_model_files did: the paths it materialized from archives
 * (ephemeral working copies, candidates for cleanup_ephemeral_files) vs
 * how many of the requested paths were already loose on disk.
 */
export type EnsureOutcome = { extracted: string[]; already_loose: number }
export type ExtensionStat = { extension: string; file_count: number; total_size_bytes: number }
export type FailedStatus = { job_id: string; error: string }
export type FilePose = { name: string; variant?: string | null; pose?: string | null; support_status?: string | null }
/**
 * A user's per-file pose assignment for a "dump everything in one folder"
 * model. Purely metadata (keyed by path, rescan-safe): the file stays put
 * on disk, but the catalog fans the folder out into one member per pose.
 */
export type FileVariant = { path: string; dir_path: string; variant: string | null; pose: string | null; support_status: string | null }
/**
 * Lava/river channels: low-frequency noise, absolute-valued so its
 * zero-crossings become winding channel centerlines and its peaks become
 * raised crusted banks.
 */
export type FlowLayer = { enabled?: boolean; channel_width_mm: number; meander_scale: number; 
/**
 * Sharpens the channel-to-bank transition (a power curve), not an
 * overall scale — `amount` is still the one knob every layer shares
 * for that.
 */
bank_height: number; amount?: number }
/**
 * A generated piece kind — the only source scatter can actually place
 * today (docs/SCATTER.md "Execution phases": bundled/user assets are S4).
 * Serializes lowercase ("pebble"/"rock"/"twig"/"grass"/"mushroom")
 * to match scatter_landscape.py's generated-kind set exactly. `Pebble`/
 * `Rock` are built as noise-displaced icospheres and still live in that
 * script's `CANONICAL_MM` table; `Twig`/`Grass`/`Mushroom` are
 * swept/extruded solids (see `build_twig_piece`/
 * `build_grass_piece`/`build_mushroom_piece` there) — same dispatch shape,
 * different geometry recipe per kind. `Mushroom` is the one kind that
 * deliberately stands upright (stem down) rather than lying flat — see
 * scatter_landscape.py's "lies_flat" comment block for why every other
 * non-round generated kind lies flat and this one doesn't.
 */
export type GeneratedPieceKind = "pebble" | "rock" | "twig" | "grass" | "mushroom"
/**
 * A named, ready-to-generate parameter set (docs/BASECUTTER.md: "Presets
 * are parameter sets" — the cutter-library move again, a new terrain
 * style is a new row here, not a new pipeline).
 */
export type GeneratorPreset = { id: string; label: string; params: LandscapeParams }
export type GeometryCancelledStatus = { job_id: string }
export type GeometryCompletedStatus = { job_id: string; mined: number; already_known: number; failed: number }
export type GeometryFailedStatus = { job_id: string; error: string }
export type GeometryProgressStatus = { job_id: string; processed: number; total: number }
/**
 * Height/volume bounds for the geometry facet, bundled into one struct
 * rather than four loose args: specta's SpectaFn tops out at 10 function
 * parameters, and search_catalog_groups (app_handle, query, tags,
 * designer, sort, limit, offset) was already six away from that ceiling.
 */
export type GeometryRange = { height_min_mm: number | null; height_max_mm: number | null; volume_min_mm3: number | null; volume_max_mm3: number | null }
export type GeometryStartedStatus = { job_id: string }
export type GeometryStatus = { Started: GeometryStartedStatus } | { Progress: GeometryProgressStatus } | { Completed: GeometryCompletedStatus } | { Failed: GeometryFailedStatus } | { Cancelled: GeometryCancelledStatus }
/**
 * Emissive glow spec for `MaterialPalette.glow` — lava-only today (the
 * crust needs a stones layer to key `glow_w` off of, see
 * gen_landscape.py's `_paint_landscape`), but the shape itself doesn't
 * assume that; any future preset can opt in the same way.
 */
export type GlowSpec = { 
/**
 * "#rrggbb", sRGB — same hex convention as the other palette fields.
 */
color: string; strength: number }
/**
 * One (designer, release_name) origin among the models a group rename would
 * touch. group_renames is keyed purely on the scanner-derived group name —
 * no root/designer scoping — so a generic name ("Spear") reused by an
 * unrelated designer collides with it silently. More than one distinct
 * origin here means the rename reaches further than whatever single card
 * the user is looking at.
 */
export type GroupOrigin = { designer: string | null; release_name: string | null; model_count: number }
/**
 * One soft-removed folder, for the Settings list.
 */
export type IgnoredFolder = { dir_path: string; ignored_at: number }
export type ImportOutcome = { release_name: string; designer: string; 
/**
 * The directory the release landed in.
 */
dest_dir: string; 
/**
 * True when the release already existed and this run updated it.
 */
updated: boolean; components: number; files: number; 
/**
 * Per-component problems (checksum mismatch, missing archive, packed at
 * rest); the rest of the release still imports.
 */
errors: string[]; 
/**
 * Non-fatal notes, e.g. locally edited files kept aside as "(edited)".
 */
warnings: string[] }
export type LandscapeGenCancelledStatus = { job_id: string }
export type LandscapeGenFailedStatus = { job_id: string; message: string; 
/**
 * Last ~10 lines of Blender stdout — a post-mortem when the failure
 * wasn't a clean GENERATION_FAILED token (e.g. a crash before the
 * script's own try/except, or an exit with no GENERATED at all).
 */
stdout_tail: string }
export type LandscapeGenFinishedStatus = { job_id: string; out_path: string; 
/**
 * The GLB twin's path (VTT GLB export design doc convention 4) — see
 * gen_landscape.py's GENERATED token and LandscapeToken::Generated.
 */
glb_path: string | null; dims_mm: [number, number, number]; manifold: boolean }
export type LandscapeGenStartedStatus = { job_id: string; seed: number }
/**
 * Landscape generator job progress — see docs/BASECUTTER.md "The landscape
 * generator (phase 6)". Deliberately its own stream, not folded into
 * BaseCutStatus: generation and cutting are different activities (one
 * bakes a heightfield, one cuts plugs from an existing one) that merely
 * share the one Blender process slot, so they get separate single-job
 * guards AND separate event families, same reasoning as BatchRenderStatus
 * getting its own stream instead of piggybacking on RenderStatus.
 */
export type LandscapeGenStatus = { Started: LandscapeGenStartedStatus } | { Finished: LandscapeGenFinishedStatus } | { Failed: LandscapeGenFailedStatus } | { Cancelled: LandscapeGenCancelledStatus }
/**
 * All style layers, every one individually optional and summable. Field-
 * level `#[serde(default)]` (each layer type's own `Default`, which sets
 * `enabled: false`) means a preset JSON can omit whole layers outright.
 */
export type LandscapeLayers = { noise?: NoiseLayer; ripples?: RipplesLayer; stones?: StonesLayer; boulders?: BouldersLayer; flow?: FlowLayer; camber?: CamberLayer }
/**
 * The frontend-facing (and preset-carried) parameter set. Deliberately has
 * no `out` field — `start_landscape_generation` derives the output path
 * (app data dir) and injects it into the wire JSON the same way
 * `job::job_json_with_cut_footprints` injects "cut" for base_cut.py.
 */
export type LandscapeParams = { seed: number; width_mm: number; depth_mm: number; resolution_mm?: number; 
/**
 * One "zoom" for the terrain itself, distinct from resolution_mm's
 * mesh density: multiplies every layer's characteristic length (stone
 * cells+gaps, ripple wavelength, noise feature size, boulder sizes,
 * channel width) so "same terrain, chunkier/finer" is one knob, not
 * five consistent edits.
 */
feature_scale?: number; carrier_mm?: number; relief_mm: number; layers?: LandscapeLayers; 
/**
 * Vertex-color/material palette for the GLB twin. `#[serde(default)]`
 * so old preset JSON / saved custom params without a "palette" key
 * still deserialize (neutral grey — see `MaterialPalette::default`).
 */
palette?: MaterialPalette }
/**
 * A magnet as it will be pocketed into a plinth's boss. Drawn from the
 * user's magnet inventory (app settings), never from a hardcoded
 * base-size->magnet table — pairing is a suggestion rule over inventory.
 * Consumed by `basecutter::job::BaseCutJob` (phase 3), which serializes it
 * straight into a placement's `magnet` field for base_cut.py.
 */
export type MagnetSpec = { diameter_mm: number; height_mm: number; count: number }
/**
 * Vertex-color/material palette for the landscape's GLB twin (VTT GLB
 * export design doc: "Palette contract"). Hex strings are sRGB; the
 * script converts to linear only where a shader node's `default_value`
 * needs it (the "Col" corner attribute itself takes sRGB directly via
 * `.color_srgb`). `glow` is only set for terrains that actually want an
 * emissive material slot (stlpack_glow) — everything else gets just
 * stlpack_terrain + stlpack_base.
 */
export type MaterialPalette = { 
/**
 * Dominant terrain color.
 */
ground: string; 
/**
 * Stones/boulders/relief-high tint, blended in by accent_w.
 */
accent: string; 
/**
 * Skirt/bottom/plinth "plastic" color — uniform, no vertex paint.
 */
base: string; glow?: GlowSpec | null }
export type MdCancelled = { job_id: string }
export type MdFailed = { job_id: string; error: MinihoardError }
export type MdFileProgress = { job_id: string; id: number; bytes_done: number; 
/**
 * Absent when the server didn't send a Content-Length.
 */
bytes_total: number | null }
export type MdFinished = { job_id: string; ok: number; failed: number }
export type MdObjectDone = { job_id: string; id: number; 
/**
 * Final on-disk release folder (post flatten/rename).
 */
dir: string }
export type MdObjectFailed = { job_id: string; id: number; reason: string }
export type MdObjectStart = { job_id: string; id: number; name: string; index: number; total: number }
export type MdStarted = { job_id: string; 
/**
 * Number of objects requested — the queue can render rows before the first
 * `object_start` arrives.
 */
total: number }
/**
 * Streaming status for a typed download run, shaped like `BaseCutStatus`: a
 * `Started`, a run of per-object events, then exactly one terminal
 * (`Finished` | `Failed` | `Cancelled`). User cancel is `Cancelled`, never
 * `Failed`. Every variant carries `job_id` so the queue UI can route it.
 */
export type MinihoardDownloadStatus = { Started: MdStarted } | { ObjectStart: MdObjectStart } | { FileProgress: MdFileProgress } | { ObjectDone: MdObjectDone } | { ObjectFailed: MdObjectFailed } | { Finished: MdFinished } | { Failed: MdFailed } | { Cancelled: MdCancelled }
/**
 * One library object, mirroring minihoard's `list --json` `entry` payload.
 */
export type MinihoardEntry = { id: number; name: string; creator: string | null; creator_username: string | null; source: string | null; library_added_at: string | null; 
/**
 * Publication date (ISO-8601), when known — the fallback "when is this
 * from?" date for the ~88% of entries without a tribe release month.
 */
published_at: string | null; 
/**
 * Creation date (ISO-8601) — always present; the last-resort date.
 */
created_at: string | null; yearmonth: string | null; tags: string[]; downloaded: boolean }
/**
 * Errors from the typed commands, shaped so the frontend branches on `kind`
 * (a discriminated union in the generated bindings), never on message text —
 * the exact coupling the old console's hardcoded English strings created.
 */
export type MinihoardError = 
/**
 * No website session cookie is stored — offer `sync-cookie`.
 */
{ kind: "cookie_missing"; message: string } | 
/**
 * The stored cookie no longer authenticates — offer `sync-cookie`.
 */
{ kind: "cookie_expired"; message: string } | 
/**
 * OAuth isn't set up (login lives in a real terminal).
 */
{ kind: "auth"; message: string } | 
/**
 * minihoard has no usable config yet.
 */
{ kind: "config"; message: string } | 
/**
 * The binary is missing, too old, or otherwise unusable.
 */
{ kind: "unavailable"; message: string } | 
/**
 * Anything else minihoard reported.
 */
{ kind: "failed"; message: string }
export type MinihoardFinished = { job_id: string; success: boolean; 
/**
 * Present when the process couldn't run at all (spawn failure).
 */
error: string | null }
/**
 * Install + auth health, mirroring minihoard's `status --json`. Non-mutating;
 * `cookie_present` is presence only — validity is proven by a real `list`.
 */
export type MinihoardHealth = { version: string; oauth_ok: boolean; username: string | null; cookie_present: boolean; library_dir: string | null }
export type MinihoardInfo = { path: string; version: string; 
/**
 * Whether this build speaks the `--json` protocol (>= 0.4.0). Below that,
 * the view falls back to the legacy raw console with an "update" hint —
 * the typed library UI has nothing to talk to.
 */
supports_json: boolean }
export type MinihoardLine = { job_id: string; line: string; 
/**
 * stderr lines render dimmer — minihoard uses stderr for progress
 * chatter, stdout for the actual answers.
 */
is_err: boolean }
/**
 * One object's on-demand detail (minihoard `object <id> --json`) — the bits a
 * library row needs when expanded. Fetched lazily, one row at a time.
 */
export type MinihoardObject = { id: number; name: string; 
/**
 * The official MyMiniFactory page (open externally).
 */
url: string | null; 
/**
 * A small (~230px) preview image URL.
 */
thumbnail_url: string | null; 
/**
 * A larger (~720px) image URL.
 */
image_url: string | null }
export type MinihoardStatus = { Line: MinihoardLine } | { Finished: MinihoardFinished }
/**
 * One manifest file this library doesn't own anywhere by checksum.
 */
export type MissingFile = { name: string; size_bytes: number }
/**
 * x/y/z are bbox extents; open_edges None means skipped, not clean.
 */
export type ModelFileGeometry = { file_name: string; tri_count: number; x_mm: number; y_mm: number; z_mm: number; volume_mm3: number; open_edges: number | null }
export type ModelLocation = { Local: string } | { External: string }
/**
 * The user-editable metadata for one model, saved together from the drawer.
 * A struct rather than positional args because specta caps command arity.
 */
export type ModelMetaUpdate = { custom_name: string | null; variant: string | null; pose: string | null; scale: string | null; support_status: string | null; release_date: string | null; designer: string | null; sculptor: string | null; release_name: string | null; base_round_mm?: string | null; base_square_mm?: string | null }
export type ModelReference = { id: string; location: ModelLocation }
/**
 * One requested directory move: rename `from` to `to` on disk and repoint
 * the catalog index to match.
 */
export type MoveOperation = { from: string; to: string }
/**
 * Base terrain: stacked-octave noise, optionally ridged for sharp crests.
 */
export type NoiseLayer = { enabled?: boolean; 
/**
 * Frequency multiplier — bigger scale = smaller, more numerous
 * features (matches Blender's own Noise Texture node convention).
 */
scale?: number; octaves?: number; 
/**
 * abs/1-abs transform per octave for sharp mountain crests instead of
 * rolling hills.
 */
ridged?: boolean; amount?: number }
/**
 * Everything the normalizer wants to do to ONE model group — shown to the
 * user as a reviewable diff before anything moves.
 */
export type NormalizeGroupPlan = { group_name: string; designer: string; target_dir: string; ops: NormalizeOp[]; 
/**
 * Source dirs to sweep (if emptied) after the moves.
 */
old_dirs: string[]; 
/**
 * Human-readable caveats (name clashes, folders left in place).
 */
notes: string[]; clean: boolean }
/**
 * One planned normalizer action. `kind`: "dir" renames a whole folder
 * (hardlink-safe), "file" moves/renames one file, "pose" only records
 * file-level pose metadata (no filesystem side). `pose` rides along on
 * file ops when a pose-dir merge bakes the pose into the file name.
 */
export type NormalizeOp = { from: string; to: string; kind: string; pose: string | null }
export type NormalizePlan = { 
/**
 * Groups with work to do (already-clean groups are only counted).
 */
groups: NormalizeGroupPlan[]; skipped: NormalizeSkip[]; total_ops: number; clean_groups: number; 
/**
 * Names of the already-clean groups — finalize can be re-run on these
 * to refresh their model.json sidecars without moving anything (the
 * repair path when sidecar-writing logic improves after a cleanup).
 */
clean_names: string[] }
export type NormalizeSkip = { group_name: string; reason: string }
export type NsfwAccessState = { unlocked: boolean; pin_configured: boolean; recovery_configured: boolean }
export type NsfwPinSetup = { state: NsfwAccessState; 
/**
 * Shown once. Only a verifier is retained locally.
 */
recovery_code: string }
export type PackCancelledStatus = { job_id: string; succeeded: number }
export type PackCompletedStatus = { job_id: string; action: string; succeeded: number; total_models: number; 
/**
 * Loose files left in place because they changed since compression —
 * surfaced so nothing silently stays behind.
 */
kept_files: string[]; elapsed_seconds: number }
export type PackFailedStatus = { job_id: string; error: string; 
/**
 * Models finished before the failure — their state change is real and
 * re-running the batch resumes after them.
 */
succeeded: number }
export type PackProgressStatus = { job_id: string; 
/**
 * "compress" | "verify" | "extract" — what the current model is doing.
 */
phase: string; current_model: string; 
/**
 * 1-based position of the current model in the batch.
 */
model_index: number; total_models: number; processed_size_kb: number; total_size_kb: number; percent: number }
export type PackStartedStatus = { job_id: string; 
/**
 * "pack" or "unpack" — one event stream serves both directions.
 */
action: string; total_models: number }
export type PackStatus = { Started: PackStartedStatus } | { Progress: PackProgressStatus } | { Completed: PackCompletedStatus } | { Failed: PackFailedStatus } | { Cancelled: PackCancelledStatus }
/**
 * What opening a `release.3pk` would do — feeds the selective-import dialog
 * before anything touches the disk.
 */
export type PackageInspection = { release_name: string; designer: string; date: string; version: string; dest_dir: string; 
/**
 * The release dir already holds a manifest — importing means updating.
 */
is_update: boolean; 
/**
 * Set when the destination exists but wasn't written by an import (no
 * readable manifest.json) — importing is refused rather than guessed at.
 */
blocked: string | null; components: ComponentStatus[] }
/**
 * One entry in `ScatterParams.pieces`: a piece source plus its relative
 * pick weight (`scatter_landscape.py::pick_piece_kind` draws by weight from
 * the accepted, non-zero-weight entries). `weight` defaults to 1.0 when
 * omitted, mirroring the script's own `entry.get("weight", 1.0)`.
 */
export type PieceChoice = { piece: ScatterPieceSource; weight?: number }
/**
 * One cut instance: a cutter positioned on the landscape. Mirrors a job's
 * `placements[]` entry (see base_cut.py's docstring) — `name` is a
 * user-facing label echoed back in the script's `CUT_*` tokens so progress
 * events can name the base, not just its index.
 * Carried as a `Vec<Placement>` by `basecutter::job::BaseCutJob` (phase 3).
 */
export type Placement = { cutter: CutterKind; x_mm: number; y_mm: number; rotation_deg: number; 
/**
 * None = no magnet pocket. Suggested from the inventory, overridable
 * per placement.
 */
magnet: MagnetSpec | null; name: string | null }
/**
 * Tapered plinth profile. Defaults are caliper-measured off a real 32 mm
 * round base (32 -> 30 mm over 3.7 mm tall, 1.2 mm wall — see
 * docs/BASECUTTER.md "The plinth"), not arbitrary round numbers.
 * Carried one-per-job by `basecutter::job::BaseCutJob` (phase 3).
 */
export type PlinthParams = { height_mm: number; taper_deg: number; 
/**
 * Open-bottom shell (wall + top plate) vs. a solid plug. Hollow saves
 * material and prints support-free; solid stays available as a flag.
 */
hollow: boolean; wall_mm: number; top_mm: number; 
/**
 * Pocket-to-magnet fit: FDM prints holes 0.1-0.25 mm undersized, resin
 * differs again, so this stays a user-visible, per-job knob.
 */
magnet_clearance_mm: number }
export type PlinthRepairSummary = { repaired: number; unchanged: number; warnings: string[] }
export type ProgressStatus = { job_id: string; processed_files: number; total_files: number; processed_size_kb: number; total_size_kb: number; percent_size: number; percent_files: number; current_file: string }
export type ProvisionCancelledStatus = { job_id: string }
export type ProvisionCompletedStatus = { job_id: string; info: BlenderInfo }
export type ProvisionExtractingStatus = { job_id: string; 
/**
 * "verify" | "extract" | "install" — mirrors PackProgressStatus.phase.
 */
phase: string }
export type ProvisionFailedStatus = { job_id: string; error: string }
export type ProvisionProgressStatus = { job_id: string; 
/**
 * f64 like wasted_bytes above — u32 caps at 4 GB.
 */
downloaded_bytes: number; total_bytes: number; percent: number }
export type ProvisionStartedStatus = { job_id: string; 
/**
 * The pinned Blender being installed, e.g. "5.1.2".
 */
version: string }
export type RecompileOutcome = { release_name: string; designer: string; dest_dir: string; components: RecompiledComponent[]; 
/**
 * Components a library donor couldn't help either (e.g. packed at
 * rest).
 */
errors: string[]; warnings: string[] }
/**
 * One component's outcome from `recompile_release`.
 */
export type RecompiledComponent = { name: string; 
/**
 * True when every manifest file for this component landed on disk —
 * via the archive or via library donors. `files_landed` can be > 0
 * even when this is false: "not complete" isn't "nothing happened".
 */
complete: boolean; files_landed: number; files_missing: number; missing_bytes: number }
export type Release = { name: string; designer: string; description: string; date: string; version: string; model_references: ModelReference[]; groups: string[]; release_dir: string; images: string[]; other_files: string[] }
/**
 * A WIP release sitting in the scratch dir, not yet packed — surfaced so
 * the builder can resume it without depending on the localStorage draft
 * snapshot surviving. Successful finalize deletes the scratch folder, so
 * anything found here is by definition unfinished.
 */
export type ReleaseDraftSummary = { release_dir: string; name: string; designer: string; model_count: number }
/**
 * One distinct release_name found across scanned models — a read-only
 * aggregation over already-indexed data, NOT a persisted publish log (the
 * catalog doesn't track a "finalized" event, so this just reflects what
 * scanning found on disk).
 */
export type ReleaseSummary = { release_name: string; designer: string | null; model_count: number; total_size_bytes: number }
export type RenderCancelledStatus = { job_id: string }
/**
 * One member the batch renderer could target. `parts` are the member's
 * loose .stl paths; packed members surface with `packed: true` and no
 * extraction (batch is a throughput feature for loose libraries — packed
 * models render fine individually via the drawer).
 */
export type RenderCandidate = { dir_path: string; variant_key: string | null; name: string; parts: string[]; 
/**
 * Stored orientation "x,y,z", if the studio ever saved one.
 */
rotation: string | null; has_preview: boolean; packed: boolean }
export type RenderCompletedStatus = { job_id: string; output_path: string; elapsed_seconds: number }
export type RenderFailedStatus = { job_id: string; error: string }
export type RenderOptions = { 
/**
 * Euler XYZ rotation in degrees, matching render_mini.py --rotate
 */
rotate: [number, number, number]; 
/**
 * Linear RGB resin base color, matching --color (None = locked look default)
 */
color: [number, number, number] | null; azimuth: number | null; elevation: number | null; zoom: number | null; resolution: number | null; samples: number | null; 
/**
 * Tonal look: "flat", "resin", "rich", or "marmoset" (Toolbag-style contrast)
 */
look: string | null; 
/**
 * Output PNG path (None = next to the first STL part)
 */
output_path: string | null; 
/**
 * Allow replacing an existing file; when false an existing output gets
 * a unique -N suffix instead of being clobbered
 */
overwrite?: boolean; 
/**
 * Re-seat parts exported around different origins by stacking them on
 * the part named *base* (render_mini.py --align-parts)
 */
align_parts?: boolean; 
/**
 * JSON overrides for the script's LOOK recipe, passed verbatim as
 * --config. Knob paths and defaults mirror src/utils/renderLookSchema.ts
 */
look_config?: string | null; 
/**
 * Geometry-driven translucent resin: thickness-dependent SSS plus a
 * warm rear-light boost. Intended for thin wings, cloth and foliage.
 */
translucent?: boolean; 
/**
 * Render the configured scale-reference figure beside the model
 * (settings supply the STL path + height; silently off when unset).
 */
scale_reference?: boolean; 
/**
 * Stand the model on a standard tapered wargaming base — the hobby's
 * own "banana for scale" (docs/BASECUTTER.md "Synergy: standard bases
 * in the Render tool"). The NOMINAL (bottom-face) footprint the user
 * picked from `basecutter::cutters::get_cutter_library`; None = no
 * base. Rust derives the cut (top-face) footprint via `top_face_of`
 * before this ever reaches the script — see
 * `render::engine::build_render_command`.
 */
base?: CutterKind | null }
export type RenderProgressStatus = { job_id: string; current_sample: number; total_samples: number; percent: number }
export type RenderStartedStatus = { job_id: string; output_path: string }
export type RenderStatus = { Started: RenderStartedStatus } | { Progress: RenderProgressStatus } | { Completed: RenderCompletedStatus } | { Failed: RenderFailedStatus } | { Cancelled: RenderCancelledStatus }
/**
 * Windswept sand: a directional sine wave, its phase distorted by a slow
 * noise field when `waviness` > 0 so ripples meander instead of ruling
 * dead-straight lines.
 */
export type RipplesLayer = { enabled?: boolean; wavelength_mm?: number; direction_deg?: number; amount?: number; waviness?: number }
export type ScanCancelledStatus = { job_id: string }
export type ScanCompletedStatus = { job_id: string; total_files: number; total_models: number; elapsed_seconds: number }
export type ScanFailedStatus = { job_id: string; error: string }
export type ScanProgressStatus = { job_id: string; files_indexed: number; current_dir: string }
export type ScanStartedStatus = { job_id: string; root: string }
export type ScanStatus = { Started: ScanStartedStatus } | { Progress: ScanProgressStatus } | { Completed: ScanCompletedStatus } | { Failed: ScanFailedStatus } | { Cancelled: ScanCancelledStatus }
export type ScatterAsset = { id: string; label: string; source: ScatterAssetSource; path: string; footprint_mm: number; height_mm: number; 
/**
 * The sRGB hex a placed instance of this asset paints its "Col" corner
 * attribute with (VTT GLB export design doc "Scatter") — a curated,
 * muted tabletop tone per bundled id (see `scatter_assets::BUNDLED_ASSETS`),
 * or `"#9a9a9a"` for every user-library asset (no curation pass has
 * looked at it, so a neutral grey is the honest default rather than a
 * guess). Threaded into a scatter job's `asset_colors` map the same
 * way `path` is threaded into `asset_paths` — see
 * `scatter_assets::resolve_asset_color`.
 */
color: string; 
/**
 * Additive to the pinned shape (docs/SCATTER.md "Scale anchor": "the
 * user-library scan applies the same lens: it warns (not blocks) when
 * a piece's footprint suggests it's a mini, not debris"). `None` for
 * every bundled asset (curated and normalized, never warns) and for a
 * user-library piece under the heuristic; `Some(message)` is advisory
 * only — the piece is still usable, never dropped from the returned
 * list on this account alone. See
 * `scatter_assets::MINI_FOOTPRINT_WARNING_MM` for the exact threshold
 * and reasoning, and `scatter_assets::unparseable_stl_warning` for the
 * other case this field carries (a file that failed to parse at all).
 */
warning: string | null }
/**
 * Where a bundled/user-library scatter asset lives, at scan time
 * (docs/SCATTER.md "Bundled assets" / "Scale anchor"). `footprint_mm` and
 * `height_mm` are measured once at curation/scan, not user input.
 */
export type ScatterAssetSource = "bundled" | "user"
export type ScatterCancelledStatus = { job_id: string }
export type ScatterFailedStatus = { job_id: string; message: string; 
/**
 * Last ~10 lines of Blender stdout — a post-mortem when the failure
 * wasn't a clean SCATTER_FAILED token (e.g. a crash before the
 * script's own try/except, or an exit with no SCATTER_DONE at all).
 */
stdout_tail: string }
export type ScatterFinishedStatus = { job_id: string; out_path: string; placed: number; manifold: boolean; 
/**
 * The scattered output's GLB twin path (VTT GLB export design doc
 * convention 4) — see scatter_landscape.py's SCATTER_DONE token and
 * `basecutter::scatter::ScatterToken::Done`.
 */
glb_path: string | null }
/**
 * A scatter job, as sent from the frontend and forwarded to
 * scatter_landscape.py verbatim — unlike `BaseCutJob`, no field is renamed:
 * the script reads `job["landscape_path"]`, `job["out_path"]`,
 * `job["layers"]` directly (see its module docstring's job JSON example).
 * 
 * `layers` is a STACK, not a single pass (docs/SCATTER.md "Layers — build
 * the debris up, peel it back"): each entry is a full `ScatterParams`, and
 * each places independently onto the TERRAIN from its own seed — adding or
 * removing a layer never moves another layer's pieces. Must be non-empty;
 * `start_scatter`/`validate_scatter_job` reject an empty stack before any
 * Blender work, same as an empty `pieces` list within one layer. One layer
 * is the common case. This replaces the old `params: ScatterParams` shape
 * outright — no compat branch, per house rule (old === redundant).
 */
export type ScatterJob = { landscape_path: string; out_path: string; layers: ScatterParams[] }
/**
 * Scatter placement parameters — see docs/SCATTER.md "Pinned interfaces"
 * and "Scale anchor: 28-32mm heroic". Defaults mirror
 * scatter_landscape.py's `scatter()`'s own `params.get(key, default)`
 * fallbacks exactly, so a partial JSON (from a preset or an older UI build)
 * behaves identically whether the default is applied here or in the
 * script. `seed`, `density_per_dm2`, and `pieces` have no script-side
 * default (missing keys raise `KeyError`/`ValueError` there), so they stay
 * required here too.
 */
export type ScatterParams = { seed: number; density_per_dm2: number; scale?: [number, number]; scale_factor?: number; sink_mm?: [number, number]; align_to_surface?: boolean; max_slope_deg?: number; edge_margin_mm?: number; 
/**
 * Clustering bias for candidate placement, `0.0..=1.0` (see
 * scatter_landscape.py's `build_candidates`/`_clump_cluster_centers`
 * for the algorithm this drives). `0.0` (the default) is the original
 * even jittered-grid behavior EXACTLY — no warp step runs at all, so a
 * job that omits this key places identically to before it existed.
 * Toward `1.0`, candidates are pulled toward a handful of seeded
 * cluster centers instead of staying evenly spread, so pieces read as
 * tufts/patches (grass clumps, forest-floor drifts) rather than a
 * uniform scatter. Deterministic and per-layer, same as every other
 * knob here — see `validate_layer` for the range check.
 */
clump?: number; pieces: PieceChoice[] }
/**
 * One piece's source — externally tagged with NO `#[serde(tag = ...)]`
 * (Rust's default enum-with-struct-variants shape), matching
 * docs/SCATTER.md's pinned `PieceChoice.piece` shape verbatim:
 * `{"Generated": {"kind": "pebble"|"rock"}}` or `{"Asset": {"id": "..."}}`.
 * scatter_landscape.py's `validate_pieces` docstring calls this out by
 * name: "matches Rust's default serde derive (no #[serde(tag=...)])".
 * `Asset` is a recognized, well-formed part of the shape today even though
 * the script fails it gracefully (S4 not implemented yet) — see
 * `validate_pieces` in scatter_landscape.py.
 */
export type ScatterPieceSource = { Generated: { kind: GeneratedPieceKind } } | { Asset: { id: string } }
export type ScatterProgressStatus = { job_id: string; placed: number; total: number }
/**
 * Rim fate for a scatter piece (a loose shell in the landscape — see
 * docs/SCATTER.md "Pieces are placed as LOOSE SHELLS") that straddles a
 * cutter's rim, per docs/BASECUTTER.md's pinned `BaseCutJob.scatter_rim`.
 * A bare lowercase JSON STRING (`"keep"` / `"slice"`), not a tagged
 * object like `CutterKind` — matches base_cut.py's `job.get("scatter_rim",
 * "keep")` verbatim.
 * 
 * `Keep` (the default — see `Default` below): per cut, only the terrain
 * shell is intersected with the cutter prism; separately, every piece
 * whose centroid lies inside that placement's derived cut footprint is
 * unioned in WHOLE and may overhang the rim like real hand-made scenic
 * basing. `Slice`: nearby piece copies are clipped per cut and appended as
 * loose slicer-friendly shells — a piece straddling a rim is sliced flush
 * without Boolean-unioning the full scattered landscape first.
 * A landscape with nothing scattered onto it (a plain generated bake, a
 * designer sculpt) is a single shell, so both variants behave
 * identically on it — see base_cut.py's `separate_into_shells`.
 */
export type ScatterRim = "keep" | "slice"
export type ScatterStartedStatus = { job_id: string }
/**
 * Scatter job progress — see docs/SCATTER.md "Pinned interfaces" and
 * scatter_landscape.py's own docstring for the stdout token protocol this
 * mirrors (SCATTER_START / SCATTER_PROGRESS / SCATTER_DONE /
 * SCATTER_FAILED). Deliberately its own stream, not folded into
 * BaseCutStatus or LandscapeGenStatus: scatter is a third distinct activity
 * (docs/SCATTER.md "The architectural call: scatter is a LANDSCAPE
 * TRANSFORMER") that happens to share the one Blender process slot, same
 * reasoning as LandscapeGenStatus getting its own stream instead of
 * piggybacking on BaseCutStatus. Cancellation gets its own variant rather
 * than flowing through Failed — a user-initiated stop is Cancelled, never
 * Failed, matching BaseCutStatus/LandscapeGenStatus's convention.
 */
export type ScatterStatus = { Started: ScatterStartedStatus } | { Progress: ScatterProgressStatus } | { Finished: ScatterFinishedStatus } | { Failed: ScatterFailedStatus } | { Cancelled: ScatterCancelledStatus }
export type Settings = { scratch_dir: string | null; target_dir: string | null; compression_type: CompressionType | null; chunk_size: number | null; max_compression_threads: number | null; blender_path: string | null; 
/**
 * Legacy single catalog folder. Read-only compatibility: it seeds
 * catalog_roots on first load after the multi-root update and mirrors
 * roots[0] so an older build opening the same store still works.
 */
catalog_root: string | null; 
/**
 * The catalog folders. Scans and the roots UI operate on this list;
 * entries must not nest inside each other (enforced on add).
 * serde(default): a not-yet-updated frontend sends Settings without
 * this key, and its saves must not start failing.
 */
catalog_roots?: string[] | null; 
/**
 * Optional staging target: when set, Clean up builds every group's
 * canonical layout HERE, draining the raw folders into it. Unset =
 * each group cleans up inside its own folder. Must name one of
 * catalog_roots; validated at use and cleared when its folder is
 * removed.
 */
catalog_primary_root?: string | null; 
/**
 * Studios the scanner recognizes in folder names to infer a designer.
 * Seeded from scanner::DEFAULT_DESIGNERS on first load; user-editable.
 */
known_designers: string[] | null; 
/**
 * What the catalog's print button does: "open-in-slicer" (default —
 * hand the files to the OS-default slicer app) or "reveal-folder"
 * (the drag-it-yourself flow for people juggling several slicers).
 */
print_action: string | null; 
/**
 * Release-builder fields the user asked to keep across drafts (the
 * "remember" checkboxes), keyed by field id — e.g. "designer" so
 * creators don't retype their own name every release.
 */
release_field_defaults: Partial<{ [key in string]: string }> | null; 
/**
 * Zstd level for compressed-at-rest packing (None = 3, zstd's default:
 * near-Deflate speed, much better ratio). Advanced knob; -7..=22.
 */
pack_level: number | null; 
/**
 * After a packed model's files were extracted for printing/preview,
 * remove them again once the action is done (None = true).
 */
pack_cleanup_after: boolean | null; 
/**
 * The managed Blender version (e.g. "5.1.2") whose first-run setup the
 * user last completed or dismissed. A version string, not a bool, so
 * bumping the pin re-offers the dialog exactly once.
 */
blender_setup_acknowledged?: string | null; 
/**
 * The scale-reference figure ("banana for scale"): a user-supplied STL
 * rendered in grey beside the model when the studio toggle is on. Not
 * bundled with the app — the user picks any figure they like, so no
 * third-party license rides in our binary.
 */
scale_reference_path?: string | null; 
/**
 * How tall the reference stands, in the model's own mm space
 * (None = 28 — a classic tabletop human).
 */
scale_reference_height_mm?: number | null; 
/**
 * The creator's licence file (PDF/txt/md), offered as a toggle in the
 * release builder — set once, ride along in every release.3pk.
 */
licence_path?: string | null; 
/**
 * The user's magnet inventory (docs/BASECUTTER.md "Hollow, with magnet
 * mounts"): what they actually own, e.g. 5x1, 6x2, 10x2. Base Cutter's
 * per-placement magnet panel offers one chip per entry and suggests
 * the largest whose boss fits a given base — never a hardcoded
 * base->magnet table. Seeded with common hobby sizes on first load
 * (see settings::default_magnet_inventory), same pattern as
 * known_designers. serde(default): an older store has no such key.
 */
magnet_inventory?: MagnetSpec[] | null; 
/**
 * The user's scatter asset library folder (docs/SCATTER.md "User
 * library"): a flat folder of `*.stl` pieces `scan_scatter_library`
 * reads non-recursively. None = no user library configured yet, the
 * piece picker only offers generated + bundled sources.
 * serde(default): an older store has no such key.
 */
scatter_library_dir?: string | null; 
/**
 * Triangle-count ceiling above which geometry mining skips the
 * open-edge HashMap (see catalog::stl_facts::EDGE_STATS_MAX_TRIS and
 * catalog::geometry::recommended_edge_cap). Seeded from the machine's
 * RAM on first load; never below EDGE_STATS_MAX_TRIS — a stored value
 * under that floor is clamped up on read. serde(default): an older
 * store has no such key.
 */
edge_stats_max_tris?: number | null }
export type StartedStatus = { job_id: string; total_files: number; total_size_kb: number }
/**
 * A model as the release builder stages it and `model.json` records it.
 * The rich fields mirror the scanner's ModelJson reader — this is the WRITE
 * side of metadata portability (docs/3PK.md): whatever curation the catalog
 * holds rides into the sidecar, the manifest, and back out on another
 * user's scan. All optional with defaults so old sidecars still parse.
 */
export type StlModel = { id: string | null; name: string; description: string | null; tags: string[]; images: string[]; model_files: string[]; group: string | null; variant?: string | null; pose?: string | null; scale?: string | null; support_status?: string | null; release_date?: string | null; designer?: string | null; sculptor?: string | null; release_name?: string | null; 
/**
 * Base sizes in mm as canonical dimension strings: "25" for a
 * regular base, "60x35" for an oval/rectangle (never a unit suffix).
 * Both optional — plenty of models ship without a base at all.
 * Additive to model.json/3pk.
 */
base_round_mm?: string | null; base_square_mm?: string | null; 
/**
 * Per-file pose/variant assignments (a curated dump folder), restored
 * into file_variants on scan. Names are file basenames.
 */
file_poses?: FilePose[] }
/**
 * Cobblestones: a hashed, jittered 2D Voronoi — domed stones separated by
 * a recessed mortar gap.
 */
export type StonesLayer = { enabled?: boolean; cell_mm: number; gap_mm: number; 
/**
 * 0 = flat-topped setts, 1 = fully rounded cobbles.
 */
dome: number; 
/**
 * Per-stone height variance (0 = every stone the same height).
 */
jitter: number; 
/**
 * 0 = even cobbles; towards 1, a slow field drowns low cells into open
 * lakes and fuses gaps between strongly-crusted neighbors — uneven
 * crust masses (the lava look) instead of a tiled street.
 */
cluster?: number; 
/**
 * 0 = clean Voronoi edges; towards 1, ~1-2mm wobble makes plate
 * outlines ragged/broken (needs a fine enough grid to resolve).
 */
rough?: number; amount?: number }
export type TagCount = { tag: string; count: number }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
